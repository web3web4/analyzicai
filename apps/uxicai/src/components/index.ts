// Re-export shared server components for convenience
// Note: These are server components, so they can only be used in server components
export {
  DashboardHeader,
  MobileNav,
} from "@web3web4/shared-platform/server-components";
